<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.technifutur.checkcleaning">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" android:windowSoftInputMode="adjustResize|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.SignUpActivity" />
        <activity
            android:name=".activity.BottomBarActivity"
            android:label="@string/title_activity_bottom_bar"
            android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".activity.BuildingChoiceActivity" />
        <activity android:name=".activity.CreateBuildingActivity" android:windowSoftInputMode="adjustResize|adjustPan"/>
    </application>

</manifest>